# Computing payment using function
def computepay(h,r):
    if h <= 40:
        return h * r
    else:
        return (40 * r + (h-40) * r * 1.50)

hrs = raw_input("Enter Hours:")
h = float(hrs)
rate = raw_input("Enter Rates:")
r = float(rate)

p = computepay(h,r)
print p
