# ask users two variable input and convert them to numeric variables
hrs = raw_input("Enter Hours: ")
h = float(hrs)
rate = raw_input("Enter Rate: ")
r = float(rate)

if h <= 40 :
    pay = r * h
else :
    pay = r * 40 + (r * 1.5 *  (h-40))
print pay
